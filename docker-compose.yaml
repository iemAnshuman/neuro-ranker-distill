version: '3.8'
services:
  neurorank-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount the models directory so you don't have to rebuild the image to change models
      - ./models:/app/models
    environment:
      - MODEL_PATH=/app/models/quantized_student.onnx